## Packages
date-fns | Formatting timestamps for dashboard and history logs
framer-motion | Smooth animations for dashboard cards and page transitions
lucide-react | Icons for the dashboard UI

## Notes
- API expects standard JSON responses. 
- Using TanStack query for caching and refetching.
- Assuming standard Shadcn UI components (Card, Button, Badge, Input, Table) are available or easily constructable with Tailwind.
- Zod is used for runtime validation of API responses.
